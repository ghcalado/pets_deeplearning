# pets_deeplearning
This project is an image classifier that identifies dog and cat breeds using Deep Learning. Trained on a dataset of approximately 37 breeds, the model receives a photo as input and returns the top 5 most likely breeds with their confidence scores.
